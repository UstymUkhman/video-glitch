varying vec3 vColor;

void main(void) {
  // const vec3 light = vec3(0.75);

  // if (vColor.x > light.x && vColor.y > light.y && vColor.z > light.z) {
  //   discard;
  // }

  // gl_FragColor = vec4(vColor, 1.0);
  gl_FragColor = vec4(vec3(0.0), 0.8);
}
